# PradySovereign Personal Assistant Blueprint
agent_name: "personal-assistant"

tools:
  filesystem:
    root: "./sandbox"
    allowed_ops: ["read", "write", "list"]
  email:
    domains: ["example.com"]
    max_per_hour: 50
  calendar:
    max_events_per_day: 10

sovereign_policies:
  min_risk_for_manual_approval: 0.7
  log_all_actions: true
